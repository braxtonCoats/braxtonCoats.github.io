import React from "react";
import { DittoSource } from "../lib/context";
interface DittoProviderProps {
    projectId?: string;
    variant?: string;
    source: DittoSource;
    children: React.ReactNode;
}
export declare const DittoProvider: (props: DittoProviderProps) => JSX.Element;
export {};
