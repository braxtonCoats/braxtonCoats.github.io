import { VariablesInput } from "../lib/context";
interface useDittoProps {
    projectId?: string;
    frameId: string;
    blockId?: string;
    variables?: VariablesInput;
    count?: number;
    filters?: {
        tags: string[];
    };
}
export declare const useDitto: (props: useDittoProps) => any;
export {};
